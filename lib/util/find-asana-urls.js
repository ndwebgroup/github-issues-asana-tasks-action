// @ts-check

/**
 * Extracts Asana Project and Task URLs from a blob of text.
 *
 * Used to discover the Project URL in an Issue's body.
 *
 * May eventually be used to discover a Task links
 *
 * @param {string} content Blob of text
 *
 * @returns {object} Returns an object containing arrays of Project and Task urls
 */
export function findAsanaUrls(content) {}
